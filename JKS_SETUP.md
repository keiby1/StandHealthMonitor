# Инструкция: Использование существующего JKS хранилища

## 📋 Обзор

Если у вас уже есть JKS хранилище с сертификатом, вы можете использовать его вместо сгенерированного PKCS12 хранилища. Эта инструкция поможет вам:

1. Извлечь сертификат из JKS в формат `.crt` для импорта в Windows
2. Настроить приложение для использования JKS хранилища

---

## 🔍 Шаг 1: Определение alias сертификата в JKS

Перед экспортом нужно узнать alias (имя) сертификата в вашем JKS хранилище.

### Команда для просмотра содержимого JKS:

```bash
keytool -list -v -keystore путь/к/вашему/хранилищу.jks -storepass ваш_пароль
```

**Пример:**
```bash
keytool -list -v -keystore C:\certs\mykeystore.jks -storepass mypassword
```

### Что искать в выводе:

В выводе команды найдите строку с `Alias name:` - это имя вашего сертификата.

**Пример вывода:**
```
Keystore type: JKS
Keystore provider: SUN

Your keystore contains 1 entry

Alias name: mycertificate    <-- ЭТО НУЖНО ЗАПОМНИТЬ
Creation date: 01.01.2024
Entry type: PrivateKeyEntry
...
```

**Запишите alias** (в примере это `mycertificate`) - он понадобится на следующих шагах.

---

## 📤 Шаг 2: Экспорт сертификата из JKS в .crt

Используйте команду `keytool -exportcert` для экспорта сертификата:

### Базовая команда:

```bash
keytool -exportcert -alias АЛИАС_СЕРТИФИКАТА -keystore путь/к/хранилищу.jks -storepass ваш_пароль -file путь/к/выходному/файлу.crt
```

### Пример:

```bash
keytool -exportcert -alias mycertificate -keystore C:\certs\mykeystore.jks -storepass mypassword -file C:\certs\certificate.crt
```

### Параметры команды:

- `-alias` - имя сертификата из JKS (то, что вы нашли в Шаге 1)
- `-keystore` - путь к вашему JKS файлу
- `-storepass` - пароль от JKS хранилища
- `-file` - путь к выходному файлу `.crt` (создастся автоматически)

### Если нужно экспортировать в папку проекта:

```bash
keytool -exportcert -alias mycertificate -keystore C:\certs\mykeystore.jks -storepass mypassword -file certs/certificate.crt
```

**Примечание:** Убедитесь, что папка `certs` существует в корне проекта.

---

## 🔐 Шаг 3: Импорт .crt сертификата в Windows

После экспорта сертификата импортируйте его в Windows для доверия браузера.

### Способ 1: Через двойной клик (Самый простой)

1. Найдите файл `certificate.crt` (который вы только что экспортировали)
2. **Двойной клик** на файл
3. Нажмите **"Установить сертификат..."**
4. Выберите **"Текущий пользователь"** → **"Далее"**
5. Выберите **"Поместить все сертификаты в следующее хранилище"**
6. Нажмите **"Обзор..."** → выберите **"Доверенные корневые центры сертификации"** → **"ОК"**
7. Нажмите **"Далее"** → **"Готово"**
8. Подтвердите установку → **"Да"**
9. **🔴 ВАЖНО: Закройте ВСЕ окна браузера и откройте заново!**

### Способ 2: Через certmgr.msc

1. Нажмите `Win + R`
2. Введите `certmgr.msc` и нажмите Enter
3. Перейдите в **"Доверенные корневые центры сертификации"** → **"Сертификаты"**
4. Правой кнопкой мыши на **"Сертификаты"** → **"Все задачи"** → **"Импорт..."**
5. Следуйте мастеру импорта, выбрав ваш `.crt` файл
6. Выберите хранилище **"Доверенные корневые центры сертификации"**
7. **Перезапустите браузер**

---

## ⚙️ Шаг 4: Настройка приложения для использования JKS

После экспорта сертификата нужно настроить `application.properties` для использования JKS вместо PKCS12.

### Откройте файл `src/main/resources/application.properties`

### Найдите секцию HTTPS/SSL и измените:

**Было (для PKCS12):**
```properties
# HTTPS/SSL Configuration
server.ssl.enabled=true
server.ssl.key-store=file:./certs/keystore.p12
server.ssl.key-store-password=${SSL_KEYSTORE_PASSWORD:changeit}
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=standhealthmonitor
server.port=8443
```

**Стало (для JKS):**
```properties
# HTTPS/SSL Configuration
server.ssl.enabled=true
server.ssl.key-store=file:./certs/your-keystore.jks
server.ssl.key-store-password=${SSL_KEYSTORE_PASSWORD:ваш_пароль}
server.ssl.key-store-type=JKS
server.ssl.key-alias=ваш_алиас
server.port=8443
```

### Параметры:

- `server.ssl.key-store` - путь к вашему JKS файлу (относительно корня проекта или абсолютный путь)
- `server.ssl.key-store-password` - пароль от JKS хранилища
  - ⚠️ **Рекомендуется:** Использовать переменную окружения или `application-secret.properties`
  - Пример: `server.ssl.key-store-password=${SSL_KEYSTORE_PASSWORD:ваш_пароль}`
- `server.ssl.key-store-type` - должно быть `JKS`
- `server.ssl.key-alias` - alias сертификата из JKS (то, что вы нашли в Шаге 1)

### Пример полной конфигурации:

```properties
# HTTPS/SSL Configuration
server.ssl.enabled=true
server.ssl.key-store=file:./certs/mykeystore.jks
server.ssl.key-store-password=${SSL_KEYSTORE_PASSWORD:mypassword}
server.ssl.key-store-type=JKS
server.ssl.key-alias=mycertificate
server.port=8443
```

### Использование секретного файла (рекомендуется):

Создайте или отредактируйте `application-secret.properties`:

```properties
# SSL Keystore Password
SSL_KEYSTORE_PASSWORD=ваш_пароль_от_jks
```

И в `application.properties`:
```properties
server.ssl.key-store-password=${SSL_KEYSTORE_PASSWORD:changeit}
```

⚠️ **ВАЖНО:** Убедитесь, что `application-secret.properties` добавлен в `.gitignore`!

---

## 📁 Шаг 5: Размещение JKS файла в проекте

### Вариант 1: В папке certs (рекомендуется)

1. Скопируйте ваш JKS файл в папку `certs/` проекта
2. В `application.properties` укажите:
   ```properties
   server.ssl.key-store=file:./certs/your-keystore.jks
   ```

### Вариант 2: Абсолютный путь

Если JKS находится вне проекта:
```properties
server.ssl.key-store=file:C:/certs/mykeystore.jks
```

**Примечание:** В Windows используйте `/` или `\\` в пути, но не `\`.

### Вариант 3: Переменная окружения

Можно использовать переменную окружения для пути:
```properties
server.ssl.key-store=${SSL_KEYSTORE_PATH:file:./certs/mykeystore.jks}
```

---

## ✅ Проверка настройки

После настройки:

1. **Перезапустите приложение**
2. Откройте `https://localhost:8443` (или ваш порт)
3. Браузер должен показать **замок** 🔒 **БЕЗ предупреждений** (если сертификат импортирован)

---

## 🔧 Устранение проблем

### Ошибка: "keystore.jks not found"

- Проверьте путь к JKS файлу в `application.properties`
- Убедитесь, что путь указан правильно (относительно корня проекта или абсолютный)
- Проверьте, что файл существует

### Ошибка: "Wrong password"

- Проверьте пароль в `application.properties` или `application-secret.properties`
- Убедитесь, что пароль правильный (проверьте через `keytool -list`)

### Ошибка: "Alias not found"

- Проверьте, что `server.ssl.key-alias` совпадает с alias в JKS
- Используйте `keytool -list` для просмотра всех alias в хранилище

### Ошибка: "Invalid keystore format"

- Убедитесь, что `server.ssl.key-store-type=JKS`
- Проверьте, что файл действительно является JKS хранилищем

### Браузер показывает "Небезопасное соединение"

- Убедитесь, что вы импортировали `.crt` сертификат в Windows (см. Шаг 3)
- Убедитесь, что **перезапустили браузер** после импорта
- Проверьте, что используете `https://`, а не `http://`

---

## 📝 Дополнительные команды

### Просмотр информации о сертификате в JKS:

```bash
keytool -list -v -keystore путь/к/хранилищу.jks -storepass ваш_пароль -alias ваш_алиас
```

### Просмотр всех сертификатов в JKS:

```bash
keytool -list -keystore путь/к/хранилищу.jks -storepass ваш_пароль
```

### Проверка экспортированного .crt файла:

```bash
keytool -printcert -file путь/к/certificate.crt
```

---

## 🔄 Конвертация JKS в PKCS12 (опционально)

Если вы хотите конвертировать JKS в PKCS12 формат (для совместимости):

```bash
keytool -importkeystore -srckeystore путь/к/хранилищу.jks -srcstorepass ваш_пароль -srcstoretype JKS -destkeystore certs/keystore.p12 -deststorepass ваш_пароль -deststoretype PKCS12
```

После конвертации можно использовать PKCS12 конфигурацию из основной инструкции.

---

## 📚 См. также

- `IMPORT_CERTIFICATE.md` - инструкция по импорту сертификата в Windows
- `HTTPS_SETUP.md` - общая инструкция по настройке HTTPS
- `.gitignore` - убедитесь, что JKS файлы не коммитятся в git


